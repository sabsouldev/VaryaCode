<footer class="bg-vc-dark border-t border-slate-200 mt-0">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            {# Logo + description #}
            <div>
                <a href="{{ path('app_home') }}" class="flex items-center space-x-2">
                    <img src="{{ asset('img/logo-VaryaCode.jpg') }}" alt="VaryaCode" class="h-8 w-8 rounded">
                    <span class="font-heading font-bold text-xl bg-gradient-to-r from-[#0A3BFB] to-[#4EEBA8] bg-clip-text text-transparent">VaryaCode</span>
                </a>
                <p class="mt-3 text-slate-400 text-sm">Des solutions web simples et accessibles pour les petites structures locales.</p>
                <p class="mt-2 text-slate-400 text-sm">Dordogne, Nouvelle-Aquitaine</p>
            </div>

            {# Navigation #}
            <div>
                <h4 class="font-heading font-semibold text-white mb-4">Navigation</h4>
                <ul class="space-y-2 text-sm">
                    <li><a href="{{ path('app_home') }}" class="text-slate-400 hover:text-vc-mint transition-colors">Accueil</a></li>
                    <li><a href="{{ path('app_offers') }}" class="text-slate-400 hover:text-vc-mint transition-colors">Offres</a></li>
                    <li><a href="{{ path('app_contact') }}" class="text-slate-400 hover:text-vc-mint transition-colors">Contact</a></li>
                </ul>
            </div>

            {# Contact #}
            <div>
                <h4 class="font-heading font-semibold text-white mb-4">Contact</h4>
                <ul class="space-y-2 text-sm text-slate-400">
                    <li><a href="mailto:contact@varyacode.fr" class="hover:text-vc-mint transition-colors">contact@varyacode.fr</a></li>
                    <li>Dordogne, Nouvelle-Aquitaine</li>
                </ul>
            </div>
        </div>

        <div class="border-t border-white/10 mt-8 pt-8 flex flex-col sm:flex-row justify-between items-center text-xs text-slate-400">
            <p>&copy; {{ "now"|date("Y") }} VaryaCode</p>
            <div class="flex space-x-4 mt-2 sm:mt-0">
                <a href="{{ path('app_mentions_legales') }}" class="hover:text-vc-mint transition-colors">Mentions l&eacute;gales</a>
                <a href="{{ path('app_confidentialite') }}" class="hover:text-vc-mint transition-colors">Confidentialit&eacute;</a>
                <a href="{{ path('app_cgv') }}" class="hover:text-vc-mint transition-colors">CGV</a>
            </div>
        </div>
    </div>
</footer>
