<nav class="fixed top-0 left-0 right-0 z-50 bg-white/90 backdrop-blur-md border-b border-slate-200/60">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-16">
            {# Logo #}
            <a href="{{ path('app_home') }}" class="flex items-center space-x-2">
                <img src="{{ asset('img/logo-VaryaCode.jpg') }}" alt="VaryaCode" class="h-8 w-8 rounded">
               <span class="font-heading font-bold text-xl bg-gradient-to-r from-[#0A3BFB] to-[#4EEBA8] bg-clip-text text-transparent">VaryaCode</span>
            </a>

            {# Desktop navigation #}
            <div class="hidden md:flex items-center space-x-8">
                <a href="{{ path('app_home') }}" class="text-vc-text-light hover:text-vc-text transition-colors text-sm font-medium">Accueil</a>
                <a href="{{ path('app_offers') }}" class="text-vc-text-light hover:text-vc-text transition-colors text-sm font-medium">Offres</a>
                <a href="{{ path('app_contact') }}" class="inline-block px-6 py-3 text-sm font-semibold text-white rounded-xl bg-gradient-to-r from-[#0A3BFB] to-[#4EEBA8] hover:opacity-90 transition-opacity shadow-md">Contact</a>

            {# Mobile menu button #}
            <button id="mobile-menu-toggle" class="md:hidden text-vc-text" aria-label="Menu">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
                </svg>
            </button>
        </div>

        {# Mobile menu #}
        <div id="mobile-menu" class="hidden md:hidden pb-4">
            <div class="flex flex-col space-y-3">
                <a href="{{ path('app_home') }}" class="text-vc-text-light hover:text-vc-text transition-colors py-2">Accueil</a>
                <a href="{{ path('app_offers') }}" class="text-vc-text-light hover:text-vc-text transition-colors py-2">Offres</a>
                <a href="{{ path('app_contact') }}" class="btn-primary text-center !py-2.5 !text-sm !rounded-lg">Contact</a>
            </div>
        </div>
    </div>
</nav>
