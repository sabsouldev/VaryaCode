<nav class="fixed top-0 left-0 right-0 z-50 bg-varya-dark/90 backdrop-blur-md border-b border-white/5">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-16">
            {# Logo #}
            <a href="{{ path('app_home') }}" class="flex items-center space-x-2">
                <span class="font-heading font-bold text-xl bg-gradient-to-r from-varya-green to-varya-blue bg-clip-text text-transparent">VaryaCode</span>
            </a>

            {# Desktop navigation #}
            <div class="hidden md:flex items-center space-x-8">
                <a href="{{ path('app_home') }}" class="text-varya-text-secondary hover:text-white transition-colors text-sm font-medium">Accueil</a>
                <a href="{{ path('app_offers') }}" class="text-varya-text-secondary hover:text-white transition-colors text-sm font-medium">Offres</a>
                <a href="{{ path('app_about') }}" class="text-varya-text-secondary hover:text-white transition-colors text-sm font-medium">À propos</a>
                <a href="{{ path('app_portfolio') }}" class="text-varya-text-secondary hover:text-white transition-colors text-sm font-medium">Portfolio</a>
                <a href="{{ path('app_contact') }}" class="btn-primary !px-5 !py-2.5 !text-sm">Contact</a>
            </div>

            {# Mobile menu button #}
            <button id="mobile-menu-toggle" class="md:hidden text-white" aria-label="Menu">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
                </svg>
            </button>
        </div>

        {# Mobile menu #}
        <div id="mobile-menu" class="hidden md:hidden pb-4">
            <div class="flex flex-col space-y-3">
                <a href="{{ path('app_home') }}" class="text-varya-text-secondary hover:text-white transition-colors py-2">Accueil</a>
                <a href="{{ path('app_offers') }}" class="text-varya-text-secondary hover:text-white transition-colors py-2">Offres</a>
                <a href="{{ path('app_about') }}" class="text-varya-text-secondary hover:text-white transition-colors py-2">À propos</a>
                <a href="{{ path('app_portfolio') }}" class="text-varya-text-secondary hover:text-white transition-colors py-2">Portfolio</a>
                <a href="{{ path('app_contact') }}" class="btn-primary text-center !py-2.5 !text-sm">Contact</a>
            </div>
        </div>
    </div>
</nav>
